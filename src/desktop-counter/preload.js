// preload.js — không cần expose API gì đặc biệt cho trang /counter (trang
// web hoạt động độc lập, không gọi Node.js API). File này giữ lại làm chỗ
// mở rộng sau này nếu cần (ví dụ tích hợp máy in hoá đơn qua USB), hiện tại
// để trống có chủ đích — contextIsolation vẫn bật để an toàn.
